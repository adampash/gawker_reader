<h3>Overall:</h3>
<p><%= @report.summary %></p>
<h3 class="month"><%= @report.name %> (<%= @posts.count %>)</h3>
<% @posts.each do |post| %>
  <%= render partial: 'posts/preview', locals: { post: post } %>
  <div class="comments">
    <% post.pinned_comments.each do |comment| %>
      <%= render comment %>
    <% end %>
  </div>
<% end %>
